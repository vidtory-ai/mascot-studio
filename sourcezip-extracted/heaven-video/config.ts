/**
 * Main application configuration file.
 * Use this file to tweak the application's behavior.
 */
export const AppConfig = {
  /**
   * Set to `true` to require users to log in before using the application.
   * Set to `false` to disable the login screen and automatically log in a 'guest' user.
   * This is useful for development or for deploying a public-facing demo.
   */
  LOGIN_REQUIRED: false,
};
